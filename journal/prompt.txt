INITIAL PROMPT:

Hi Gemma. I need a website that works as a virual tabletop

I want you to use the Flask library (python) tho host the web server and handle server-side functions

The website has 4 pages, accessible from the "index.html" main page

the first page allows you to upload files and create directories inside a subfolder in the server called "uploads"
in this page you can also see the whole directory-file tree created inside "uploads"
There should be also a system and button to erease files and directories
Obviously the files can be uploaded to any sub-directory created inside "uploads".

The second page is a page where inside a <textarea> tag you visualiza a json called "config.json" that is stored on the server. Below the textarea there are a "save changes", "refresh" and "go back" buttons. The "config.json" contains "general configuration information" for the fifth page (milliseconds to refresh the VTT, 3d world background color, 3d ambient light settings)

The third page is another json-edit page, but the visualized file is "status.json" 
The "status" json saves information about what is present on the fourth page
* position, orientation and size of 3d models saved in STL format (inside the "uploads" folder)
* position, size and orientation of images in PNG format (inside the "uploads" folder)

Make the Json render system Dynamic: Make an array for images and an array for 3d models that will be rendered. 
The user will be able to add or remove or edit in real time all the elements.


The fourth page is the 3d virtual tabletop. 
In this page there is a 3d ambient that uses the "three.js" javascript library to visualize everything that is stored on "status.json"
To move the camera around, use the "OrbitControl.js"
The status is update every "n" millisiecond where n is stored in config.json
The system checks if something is changed. If something is changed it is re-rendered or rotated or moved or re-scaled, if not it does nothing to that object to save computational performances

I want the website to have a dark-theme rectangle based sci-fi cutting edge graphic 

Feel free to use websocket library if youu need them and implement quality-of-life methods and functions to make the system capable of doing more things. Examples are titles of the pages, instruction manual pages or any other thing thats comes to your mind

Thank you very much for all your work Gemma

SECOND PROMPT

It works perfectly!, thank you very much

I have to ask you to add some improvements:
* add some little graphic details in the file system manager to be able to recognize better the filesystem hierarchy.
* Make the textareas in json editors bigger. Can you make them nearly-fullscreen? Remember to keep the interface responsive
* Remove the wireframe-type rendering and add the possibility to choose, inside "world.json", the color of the 3d object mesh faces.
* Add the "border" to the 3d objects (in three js is called "EdgesGeometry"). In world.json one can choose the EdgesGeometry color and the thresholdAngle

* add a flag in the "world.json" objects called "unselectable". If it is true, it cannot be selected from the virtual tabletop page, but can only be modified from the json editor page
* Add the possibility, from the vtt page, to set every parameter (contained in world.json) of a certain selected object (except the "unselectable" tag). 
When an object (3d or image) is selected with the mouse click make appear a rototranslation draggable interface like the blender one
* if with this modification system you have to rewrite the json, add a system that formats the json in a human-readable format (pretty print)

Thank you. Your help has been essential for this project so far. I coult not mkae it without you.

FUTURE IDEAS:
--> Add the sky png image
--> add distance meter between objects 
--> Add aura idicators